{
  "name": "rubicon-bathsnx",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ platocrat/rubicon-bathsnx",
    "create-local": "graph create --node http://localhost:8020/ platocrat/rubicon-bathsnx",
    "remove-local": "graph remove --node http://localhost:8020/ platocrat/rubicon-bathsnx",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 platocrat/rubicon-bathsnx"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.22.3",
    "@graphprotocol/graph-ts": "^0.22.1"
  }
}